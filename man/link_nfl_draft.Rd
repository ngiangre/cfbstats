% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/link.R
\name{link_nfl_draft}
\alias{link_nfl_draft}
\title{Bridge CFBD picks to nflverse ids and NFL outcomes}
\usage{
link_nfl_draft(picks, nfl_draft_picks)
}
\arguments{
\item{picks}{Cleaned CFBD picks from \code{\link[=clean_picks]{clean_picks()}}.}

\item{nfl_draft_picks}{Cleaned nflverse draft picks from
\code{\link[=clean_nfl_draft_picks]{clean_nfl_draft_picks()}}.}
}
\value{
\code{picks} with \code{gsis_id}, \code{pfr_player_id}, \code{nfl_to}, \code{nfl_games},
\code{nfl_seasons_started}, and the \code{nfl_matched} flag attached. Same row count
as \code{picks}.
}
\description{
Attaches nflverse player ids and headline NFL-career outcomes to CFBD \code{picks}
by \strong{draft slot} — CFBD \verb{(year, round, overall)} == nflverse
\verb{(season, round, pick)}. This is the reliable bridge: CFBD \code{nflAthleteId} is a
different id namespace (0 of 4,350 recent picks match nflverse \code{espn_id}) and
name-only matching is collision-prone, whereas the slot join matched
4,350/4,350 recent picks (99.7\% carrying a \code{pfr_player_id}); decision 0014.
}
\details{
A \code{\link[=normalize_name]{normalize_name()}} guard (as in \code{\link[=link_recruiting]{link_recruiting()}}, decision 0013) nulls
the attached ids/outcomes whenever the CFBD and PFR names disagree, flagged by
\code{nfl_matched}; benign suffix/nickname differences (Will Anderson Jr. ↔ Will
Anderson) pass. The nflverse table is unique per slot, so the join is
many-to-one and never inflates \code{picks}. Picks outside the nflverse window
(e.g. pre-2010) simply find no slot and stay unmatched.

Longevity note (decision 0014): \code{nfl_to} (last active NFL season) and
\code{nfl_games} are \strong{right-censored} for recent draft classes still active —
treat them as "≥" outcomes, not completed careers.
}
