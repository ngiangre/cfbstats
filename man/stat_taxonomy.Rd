% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stat_taxonomy.R
\name{stat_taxonomy}
\alias{stat_taxonomy}
\title{Stat-phase taxonomy}
\usage{
stat_taxonomy()
}
\value{
A tibble with columns \code{category}, \code{statType}, \code{phase}, \code{label},
\code{kind}, \code{description}.
}
\description{
Reads the canonical \verb{(category, statType) -> phase / label} mapping shipped
with the package (\code{inst/extdata/stat_taxonomy.csv}). This is the single
source of truth for making the long \code{player_stats} interpretable: it assigns
each stat a game \strong{phase} (\code{offense} / \code{defense} / \code{special_teams}), a
plain-language \code{label} (so a bare \code{"TD"} becomes e.g. "Defensive touchdowns"
vs "Passing touchdowns"), and a \code{kind} (\code{scoring} / \code{volume} / \code{rate}).
}
\details{
Coverage of every \verb{(category, statType)} pair present in \code{player_stats} is
enforced by \code{\link[=contract_stat_taxonomy]{contract_stat_taxonomy()}}, so a new stat appearing in a refresh
fails fast rather than going silently unlabeled.
}
\examples{
head(stat_taxonomy())
}
