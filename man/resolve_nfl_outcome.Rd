% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/link.R
\name{resolve_nfl_outcome}
\alias{resolve_nfl_outcome}
\title{Resolve a single player's NFL outcome (drafted or undrafted-signed)}
\usage{
resolve_nfl_outcome(subject, picks_nfl, nfl_rosters, nfl_player_stats)
}
\arguments{
\item{subject}{A one-row tibble/list with \code{playerId} (CFBD athlete id,
string), \code{player} (name), and optionally \code{college} (CFBD school name) and
\code{last_college_season} (int) used to bound the draft window and disambiguate
name collisions.}

\item{picks_nfl}{CFBD picks bridged to nflverse (\code{\link[=link_nfl_draft]{link_nfl_draft()}}).}

\item{nfl_rosters}{Cleaned NFL rosters (\code{\link[=clean_nfl_rosters]{clean_nfl_rosters()}}); must carry
\code{player_name}.}

\item{nfl_player_stats}{Cleaned NFL player-season stats
(\code{\link[=clean_nfl_player_stats]{clean_nfl_player_stats()}}); used for career games.}
}
\value{
A one-row tibble: \code{nfl_status}
(\code{drafted}/\code{undrafted-signed}/\code{no-NFL-record}), \code{nfl_match_method}
(\code{slot}/\code{name}/\code{name+college}/\code{name+window}/\code{none}), \code{gsis_id},
\code{draft_year}/\code{draft_round}/\code{draft_overall}, \code{nfl_seasons},
\code{nfl_first_season}, \code{nfl_last_season}, \code{nfl_games}, \code{right_censored}, and a
human \code{note}.
}
\description{
The general college->NFL bridge used by \code{\link[=player_dossier]{player_dossier()}}. Answers, for one
player, whether they reached the NFL and how: \strong{drafted} (via the reliable
draft-slot bridge already in \code{\link[=link_nfl_draft]{link_nfl_draft()}}) or \strong{undrafted-signed} (via
a name-guarded match against \code{nfl_rosters}, which is the only way to catch a
UDFA — there is no draft slot to key on). This is deliberately an
\strong{outcome/label} resolver kept off the model input path (leakage), and it
\strong{never fabricates a link}: an ambiguous name match (multiple candidate
players that college/entry-window can't disambiguate) resolves to
\code{no-NFL-record} rather than a guessed id.
}
\details{
Longevity (distinct NFL roster seasons, first/last season, career games) is
computed from the matched \code{gsis_id}; \code{right_censored} flags players whose last
roster season is the most recent one available (possibly still active), so
treat their longevity as a ">=" outcome (decision 0014).
}
