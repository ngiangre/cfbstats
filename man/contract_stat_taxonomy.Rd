% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/contracts.R
\name{contract_stat_taxonomy}
\alias{contract_stat_taxonomy}
\title{Data contract for the stat-phase taxonomy}
\usage{
contract_stat_taxonomy(
  player_stats,
  taxonomy = stat_taxonomy(),
  stop_on_fail = TRUE
)
}
\arguments{
\item{player_stats}{Long player-season stats with \code{category} and \code{statType};
the coverage universe the taxonomy must fully cover.}

\item{taxonomy}{The taxonomy lookup; defaults to \code{\link[=stat_taxonomy]{stat_taxonomy()}}.}

\item{stop_on_fail}{If \code{TRUE} (default), raise an error when any check fails.}
}
\value{
Invisibly, a list of the two interrogated \code{pointblank} agents
(\code{lookup}, \code{coverage}).
}
\description{
Validates the taxonomy lookup and, crucially, its \emph{coverage}: every
\verb{(category, statType)} pair present in \code{player_stats} must resolve to a
taxonomy row, so a new stat appearing in a refresh fails fast rather than
going silently unlabeled (the analogue of the conference-tier coverage
guard). Intended to run in the pipeline.
}
