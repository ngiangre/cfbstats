% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dossier.R
\name{player_dossier}
\alias{player_dossier}
\title{Assemble an interpretable player dossier}
\usage{
player_dossier(
  id,
  player_stats,
  roster,
  coaches,
  picks_nfl,
  recruiting,
  nfl_rosters,
  nfl_player_stats,
  teams = NULL,
  taxonomy = stat_taxonomy()
)
}
\arguments{
\item{id}{CFBD athlete id (coerced to string).}

\item{player_stats}{Cleaned long \code{player_stats}.}

\item{roster}{Cleaned \code{roster}.}

\item{coaches}{Cleaned \code{coaches} (FBS head coaches only).}

\item{picks_nfl}{CFBD picks bridged to nflverse (\code{\link[=link_nfl_draft]{link_nfl_draft()}}).}

\item{recruiting}{Cleaned \code{recruiting}.}

\item{nfl_rosters, nfl_player_stats}{Cleaned nflverse tables (longevity source).}

\item{teams}{Optional cleaned \code{teams} dimension (display metadata); if
supplied, the team \code{mascot} is attached to the identity block.}

\item{taxonomy}{Stat-phase taxonomy; defaults to \code{\link[=stat_taxonomy]{stat_taxonomy()}}.}
}
\value{
An object of class \code{cfb_dossier}: a named list with \code{id}, \code{identity},
\code{stats} (tidy, phase-labeled), \code{coaching}, \code{recruiting}, \code{outcome}, and
\code{provenance}.
}
\description{
Builds a structured, human-readable record for a single college player keyed
on the CFBD athlete id (the shared \code{player_stats$playerId} / \code{roster$playerId}
namespace, decision 0003). Returns a \code{cfb_dossier} object with tidy tables per
section plus a \code{print()} method for handing to a reviewer.
}
\details{
Design commitments:
\itemize{
\item \strong{Stats are grouped by phase} (offense / defense / special_teams) and
labeled via \code{\link[=stat_taxonomy]{stat_taxonomy()}}, so "TD" is disambiguated into e.g. "Passing
touchdowns" vs "Defensive touchdowns".
\item \strong{Coaching context is explicit about coverage}: the CFBD coaches feed is
FBS head-coaches only, so a player-season with no coach row is marked
\code{coach_available = FALSE} ("not in CFBD feed — non-FBS / missing"), not a
silent \code{NA}.
\item \strong{Recruiting} uses the name-guarded rating (decision 0013); a genuinely
unrated player stays "unrated" rather than imputed.
\item \strong{Outcome} is resolved by \code{\link[=resolve_nfl_outcome]{resolve_nfl_outcome()}} — drafted or
undrafted-signed — and is an outcome/label kept off the model path.
}
}
