% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/link.R
\name{player_trajectory}
\alias{player_trajectory}
\title{Assemble a player's college-through-NFL trajectory spine}
\usage{
player_trajectory(subjects, roster, nfl_rosters, nfl_draft_picks)
}
\arguments{
\item{subjects}{A registry tibble with \code{who} (display name), \code{cfb_athlete_id}
(CFBD athlete id, string), and \code{gsis_id} (nflverse id, string).}

\item{roster}{College roster source (raw \code{data/roster.parquet} schema: \code{id},
\code{season}, \code{team}).}

\item{nfl_rosters}{NFL roster source (raw \code{data/nfl_rosters.parquet} schema:
\code{gsis_id}, \code{season}, \code{team}).}

\item{nfl_draft_picks}{NFL draft-pick source (raw
\code{data/nfl_draft_picks.parquet} schema: \code{gsis_id}, \code{season}, \code{round},
\code{pick}). Lists drafted players only.}
}
\value{
A tibble, one row per subject player-season, with \code{who},
\code{cfb_athlete_id}, \code{season}, \code{team}, \code{stage} (\code{"College"}/\code{"NFL"}),
\code{drafted} (logical), and \code{draft_year}/\code{draft_round}/\code{draft_overall}
(\code{NA} for undrafted players), arranged by \code{who} then \code{season}.
}
\description{
For a small registry of subjects, builds one row per player-season across
both stages (college roster + NFL roster) and attaches a \strong{data-backed draft
status}. Draft status keys on the reliable nflverse \code{gsis_id}: presence in
\code{nfl_draft_picks} means drafted (with the draft year/round/overall attached),
absence means the player entered the NFL undrafted. Because \code{gsis_id} is
nflverse's own namespace shared by both the subject registry and the draft
table, this join needs no name guard (unlike the CFBD-side bridges in
\code{\link[=link_nfl_draft]{link_nfl_draft()}} / \code{\link[=link_recruiting]{link_recruiting()}}).
}
\details{
The source tables may be lazy \code{arrow} Datasets (the published
\verb{data/*.parquet}, read on their \strong{raw} schema — \code{roster} uses \code{id}, NFL
tables use \code{gsis_id}) or in-memory tibbles; the pipeline runs the same dplyr
verbs over either and \code{collect()}s at the end. This encapsulates the data
pull a blog post would otherwise inline (decision 0015).
}
