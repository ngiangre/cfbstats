% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ingest.R
\name{ingest_teams}
\alias{ingest_teams}
\title{Ingest team metadata (logos, colors, conference)}
\usage{
ingest_teams()
}
\value{
A flat tibble, one row per team row returned by \verb{/teams} (school names
may still repeat until \code{\link[=clean_teams]{clean_teams()}} dedupes them).
}
\description{
Pulls \verb{/teams} — the team \strong{dimension} table, one row per program with
display metadata (colors, mascot, abbreviation, conference). The raw \code{logos}
block is a list of the same image at several resolutions in light and \code{-dark}
variants; like the other ingest functions (which unnest \code{hometownInfo} /
\code{seasons}), this flattens it here into \code{logo_light} / \code{logo_dark} so the
persisted parquet stays flat. Deduping duplicate school names is a cleaning
concern, handled in \code{\link[=clean_teams]{clean_teams()}}.
}
