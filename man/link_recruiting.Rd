% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/link.R
\name{link_recruiting}
\alias{link_recruiting}
\title{Attach name-guarded high-school recruiting ratings}
\usage{
link_recruiting(player_season, recruiting)
}
\arguments{
\item{player_season}{Player-season backbone from \code{\link[=build_player_season]{build_player_season()}}
(carries the athlete \code{player} name).}

\item{recruiting}{Cleaned recruiting ratings from \code{\link[=clean_recruiting]{clean_recruiting()}}.}
}
\value{
\code{player_season} with \code{hs_stars}, \code{hs_rating}, \code{hs_national_rank},
\code{hs_class}, and \code{recruit_matched} attached. Same row count as
\code{player_season}.
}
\description{
Left-joins the per-athlete recruiting ratings (\code{\link[=clean_recruiting]{clean_recruiting()}}) onto the
player-season backbone on the string \code{playerId}, then \strong{guards the join by
name}. The recruiting \code{athleteId} collides across different people
(decision 0013) — e.g. Cam Ward's id maps to a different recruit, Xavier Ward
— so a rating is trusted only when the recruiting name agrees with the
backbone \code{player} name after \code{\link[=normalize_name]{normalize_name()}}. Ratings from a
name-conflicting (wrong-person) link are set to \code{NA}; \code{recruit_matched}
records whether a validated rating was attached. The recruiting table is one
row per \code{playerId}, so the join is many-to-one and never inflates the
backbone.
}
\details{
This is a display/feature enrichment kept off the model path for now (like
\code{\link[=link_team_meta]{link_team_meta()}}); wiring HS rating into the model is future work.
}
