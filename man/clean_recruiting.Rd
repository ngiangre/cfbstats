% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clean.R
\name{clean_recruiting}
\alias{clean_recruiting}
\title{Clean high-school recruiting ratings}
\usage{
clean_recruiting(recruiting)
}
\arguments{
\item{recruiting}{Raw recruiting tibble from \code{\link[=ingest_recruiting]{ingest_recruiting()}}.}
}
\value{
A cleaned tibble, one row per recruited athlete, keyed by \code{playerId}.
}
\description{
Standardizes the raw recruiting feed to a \strong{per-athlete} rating table keyed
by the string \code{playerId} (from \code{athleteId}). Recruits with no \code{athleteId}
cannot join the athlete-id namespace (decision 0003) and are dropped here
(~40\% of the feed: never enrolled, JUCO/international, or unlinked).
Deduplicates to one row per athlete, keeping the highest \code{rating} (then
\code{stars}, then most recent class) since a player can appear in more than one
record/class — the same dedupe philosophy as \code{\link[=clean_teams]{clean_teams()}}.
}
\details{
The recruiting \code{athleteId} is \strong{not} a reliable member of the shared
athlete-id namespace: it collides across different people (decision 0013), so
the join this table feeds must be name-guarded — see \code{\link[=link_recruiting]{link_recruiting()}}.
}
