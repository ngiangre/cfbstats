% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/contracts.R
\name{contract_teams}
\alias{contract_teams}
\title{Data contract for the team dimension}
\usage{
contract_teams(teams, player_season, stop_on_fail = TRUE)
}
\arguments{
\item{teams}{Cleaned team dimension (see \code{\link[=clean_teams]{clean_teams()}}).}

\item{player_season}{Player-season backbone (see \code{\link[=build_player_season]{build_player_season()}}); the
coverage universe the dimension must fully cover.}

\item{stop_on_fail}{If \code{TRUE} (default), raise an error when any check fails.}
}
\value{
Invisibly, a list of the two interrogated \code{pointblank} agents
(\code{dim}, \code{coverage}).
}
\description{
Validates the cleaned team dimension and, crucially, its \emph{coverage}: every
\code{team} present in the player-season backbone must resolve to a row so a
logo/color join never silently drops a program. Logo \emph{presence} is not a hard
check — a few small programs legitimately carry no logo URL (a placeholder is
used at display time), so that is left to a softer signal.
}
