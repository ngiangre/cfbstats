% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ingest.R
\name{parquet_asset}
\alias{parquet_asset}
\title{Materialize (in refresh mode) or track a parquet data asset}
\usage{
parquet_asset(path, build)
}
\arguments{
\item{path}{Destination parquet path.}

\item{build}{A zero-argument function returning the table to write (e.g. an
\verb{ingest_*()} function). Only evaluated in refresh mode.}
}
\value{
\code{path} (returned visibly so a \code{format = "file"} target can track it).
}
\description{
The pipeline's ingest targets call this (decision 0017). In \strong{refresh mode}
(\code{\link[=refresh_enabled]{refresh_enabled()}}) it calls \code{build()} and writes the result to \code{path}; in
\strong{track mode} (the default) it assumes \code{path} already exists — committed, or
downloaded from the \code{data-latest} release — and simply returns it, so the DAG
runs with no API key. Returned so a \code{format = "file"} target can track it.
}
\keyword{internal}
