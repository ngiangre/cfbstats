% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/conference_tiers.R
\name{contract_conference_tiers}
\alias{contract_conference_tiers}
\title{Data contract for the season x conference -> tier lookup}
\usage{
contract_conference_tiers(tiers, player_stats, stop_on_fail = TRUE)
}
\arguments{
\item{tiers}{Lookup table with columns \code{season}, \code{conference}, \code{tier},
\code{tier_label} (e.g. \code{data/conference_tiers.parquet}).}

\item{player_stats}{Player-season table with \code{season} and \code{conference}; the
coverage universe the lookup must fully cover.}

\item{stop_on_fail}{If \code{TRUE} (default), raise an error when any check fails.}
}
\value{
Invisibly, a list of the two interrogated \code{pointblank} agents
(\code{lookup}, \code{coverage}).
}
\description{
Validates the conference-tier lookup and, crucially, its \emph{coverage}: every
\verb{(season, conference)} pair present in \code{player_stats} must resolve to a tier,
so transfer-direction features never silently drop a conference. Intended to
run in the pipeline and fail fast (decision 0003).
}
