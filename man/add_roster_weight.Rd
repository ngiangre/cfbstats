% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/features.R
\name{add_roster_weight}
\alias{add_roster_weight}
\title{Attach roster weight}
\usage{
add_roster_weight(player_season, roster)
}
\arguments{
\item{player_season}{Player-season table keyed by \code{playerId}, \code{season}.}

\item{roster}{Cleaned roster from \code{\link[=clean_roster]{clean_roster()}} (must include \code{weight}).}
}
\value{
\code{player_season} with the static roster \code{weight} attached.
}
\description{
Joins the player's listed roster weight by athlete id and season.
}
\details{
Note (decision 0003 Outcome): the CFBD \verb{/roster} endpoint reports a \strong{static}
weight per player — repeated across every season, with no year-over-year
variation (0 of ~44k multi-season players show any change). A between-season
\code{weight_delta} is therefore not derivable from this source, so we keep the
static physical attribute only. A career-span change (e.g. recruiting vs.
draft weight) would need a different source.
}
