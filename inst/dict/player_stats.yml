dataset: player_stats
source: CFBD /stats/player/season (per year, 2010–2025)
grain: LONG — one row per player-season-category-statType-stat
key: playerId (string) + season
description: >
  Player season statistics in long format. Must be pivoted into
  position-relevant features (features stage); discovering which stats carry
  information is part of the analysis.
columns:
  - name: season
    type: int32
    description: Season year.
    provenance: raw
  - name: playerId
    type: string
    description: CFBD athlete id (string); canonical player key.
    provenance: raw
  - name: player
    type: string
    description: Player display name (QA / display only — do not join on name).
    provenance: raw
  - name: position
    type: string
    description: Player position.
    provenance: raw
  - name: team
    type: string
    description: Team; joins to head-coach-season on (team == school, season).
    provenance: raw
  - name: conference
    type: string
    description: Conference; joins to the season-aware tier lookup.
    provenance: raw
  - name: category
    type: string
    description: Stat category (e.g. passing, rushing). Pivot dimension.
    provenance: raw
  - name: statType
    type: string
    description: Stat type within category. Pivot dimension.
    provenance: raw
  - name: stat
    type: string
    description: Stat value (stored as string in the raw feed).
    provenance: raw
